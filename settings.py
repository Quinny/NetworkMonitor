# The network mask for your local network.  All IP's within this mask will
# be tracked.
NETWORK_MASK   = "192.168.1.0/24"

# The IP/hostname and port of your redis instance which will be used as the
# underlying data store.
REDIS_HOST     = "192.168.1.19"
REDIS_PORT     = 6379

# The port to run the web ui on.
WEB_UI_PORT    = 80

# (Optional) The IP address of your local DNS server (usually your router) used
# to resolve local IP addresses to hostnames.
LOCAL_DNS_ADDR = "192.168.1.1"
